#ifndef _XUTIL_H
#define _XUTIL_H

// Simulate a mouse click at the current pos.
void mouse_click(int button);

// Move mouse pointer.
void mouse_move(int x, int y);

// Get mouse coordinates.
void mouse_getpos(int *x, int *y);

#endif // header guard
